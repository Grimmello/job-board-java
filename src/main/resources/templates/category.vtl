<!--category.vtl-->

<div class="well">
  <h1>$category.getName()</h1>
</div>
<div class="well">
  #if ($category.getJobs().size() > 0)
  <ul>
    #foreach($job in $category.getJobs())
      <li><a href="/category/$category.getId()/job/$job.getID()">$job.getTitle()</a></li>
    #end
  </ul>
  #else
    <h3>No Jobs Found</h3>
  #end
</div>
<!--category.vtl-->
